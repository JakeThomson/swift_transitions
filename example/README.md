# swift_transitions example

Runnable demo app for the [`swift_transitions`](../) package.

```sh
flutter pub get
flutter run
```

iOS and macOS are the reference platforms since the transition is modelled on
iOS. The app also builds for Android and web.
