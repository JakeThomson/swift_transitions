import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder so `flutter test` has a suite to run. Replaced by real tests
  // once the public API lands.
  test('test suite runs', () {
    expect(1 + 1, 2);
  });
}
